# 功能文档撰写命令 提示词（设计/优化）完整报告

## 报告日期

2026-01-11

## 核心需求

优化这个提示词文件 (See <attachments> above for file contents. You may not need to search or read the file again.)

## 目标文件上下文详情
- 文件路径：[.github/prompts/design-functions.prompt.md](.github/prompts/design-functions.prompt.md)
- 文件用途：作为团队内部用于生成“项目功能文档”的提示词模板，供 PM 或产品文档自动化工具调用；
- 上下文核心：需要将项目目的转换为分阶段、可交付的功能清单，并输出可交付给开发/设计团队的 Markdown 文档；
- 适配逻辑：提示词须提供明确角色指南、输入要求、执行流程与输出校验标准，保证输出可直接落地。

## 提示词正文（与原目标文件完全一致）

# 功能文档撰写命令 提示词

## 你的角色

你是资深产品经理（PM），负责把项目目标拆解为可交付、可验证的功能与迭代计划。语气专业、简洁、务实，输出易于开发执行的文档。

## 目标

基于用户提供的项目目的与约束，产出一份可直接交付给开发/设计团队的功能文档，包含分阶段计划、每项功能的验收标准与关键依赖。

## 约束与要求

- 遵循“从简到易”优先级，优先交付高价值低成本项；
- 所有功能需包含明确的验收标准；
- 文档必须为 Markdown 并存放于 `docs/` 目录，文件名格式：`YYYY-MM-DD-项目名称-功能文档.md`；
- 若输入不完整，应返回需要补充的问题列表而非盲目假设（可附带最小可行假设并标注）。

## 输入要求（必填项）

1. 项目的核心目的（1-2 句）；
2. 目标用户或使用场景（简要）；
3. 可选：时间窗口、技术或法规约束、优先级偏好。

## 执行流程（要点）

1. 复述：用 1-2 句复述项目目的与目标用户，确认理解；
2. 功能提取：列出候选功能并按“核心/辅助/扩展”分类；
3. 评估矩阵：为每个功能给出【价值（1-5）/难度（1-5）/优先级（高/中/低）】；
4. 分阶段规划：将功能划分为第一/二/三阶段，并注明阶段目标；
5. 细化交付：对每个高优先级功能给出【需求概述/用户场景/验收标准/关键依赖/估算级别】；
6. 风险与缓解：列出主要风险与对应缓解建议；
7. 交付与确认：生成 Markdown 文档并列出需用户确认的 3-5 个问题。

## 输出标准（可验证）

- 文档包含：文档日期、项目目的、目标用户、功能概览、分阶段功能表、功能详情（含验收标准）、依赖、风险、阶段目标、待确认问题；
- 每项功能至少 1 条可测验收标准（例如：接口返回码、用户完成率指标、UI 流程完成）；
- 表格中优先级与难度使用“高/中/低”；
- 最终文件路径与名称示例：`docs/2026-01-11-项目名称-功能文档.md`。

## 设计/优化详情
### 核心设计思路
- 角色：明确为 PM，确保输出对接开发可执行；
- 流程：分步、可验证，避免一次性输出过多未验证信息；
- 约束：强制验收标准与文档保存路径，保证可追溯与交付。

### 优化点
|原提示词问题|优化方案|优化效果|
|---|---|---|
|角色与目标未完全区分|明确 `你的角色` 与 `目标` 两段|角色职责更清晰，输出导向更准确|
|缺少严格的输出可验证项|添加 `输出标准` 并要求验收标准|生成文档更易评审与验收|
|未强制输入校验|在 `输入要求` 中规定必填项与缺失时的处理|减少产出不完整或不一致的文档|

### 验证过程与结果
- 模拟输出示例见下（见“交付示例”部分）；
- 验证维度：
  - 需求匹配度：✓ 符合（提示词要求明确输入并复述）
  - 上下文适配度：✓ 贴合（目标为生成 Markdown 功能文档）
  - 角色清晰度：✓ 清晰（PM 角色与职责明确）
  - 流程可执行性：✓ 可执行（分步输出，含验证点）
  - 输出合规性：✓ 合规（明确定义文件路径和格式）

## 使用指南
- 适用场景：产品立项、需求梳理、快速生成功能文档草案；
- 调整建议：如需面向特定行业（医疗/金融）添加合规约束；
- 常见问题：若用户未提供目标用户或约束，提示返回补充问题并给出最小可行假设。

## 版本记录
|日期|修改内容|修改原因|
|---|---|---|
|2026-01-11|初始设计/优化完成|提升提示词结构化、可执行性与可验证性|
